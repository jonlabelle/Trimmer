{
  // Removes all whitespace characters (including newlines), from the END of
  // the document. The default value is `false`.
  "trim_eof": false,

 // Saves the current document/buffer immediately after trimming is performed.
 // The default value is `false`.
  "save_after_trim": false
}
